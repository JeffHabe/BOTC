# 角色令片自動生成與 A4 排版工具使用手冊

本手冊用以說明如何使用 `generate_tokens.py` 與 `layout_tokens.py`，自動將血染鐘樓的所有角色生成為 600 DPI 極致高清（照片列印級）的實體令片，並拼貼至 A4 畫布以供直接列印。

---

## 🛠️ 準備工作

執行本工具前，請確保您的系統滿足以下條件：
1. **Python 3**：電腦已安裝 Python 3（在命令列中輸入 `py --version` 應有正確版本顯示）。
2. **內建瀏覽器**：Windows 系統中已安裝 **Microsoft Edge** 或 **Google Chrome**（腳本會自動搜尋這兩者來進行無頭網頁渲染）。
3. **網際網路連接**：首次執行時需要下載角色頭像圖片。

---

## 🚀 兩步驟完整工作流

### 步驟一：生成單張角色令片 (`generate_tokens.py`)

本步驟會讀取角色定義 JSON，下載頭像，並使用 Edge 瀏覽器對 HTML 模板進行 $750 \times 750$ 像素的高清無頭截圖。

- **基本執行指令（全部生成）**：
  ```bash
  py generate_tokens.py
  ```
- **指定單個/多個角色局部更新**：
  ```bash
  py generate_tokens.py --roles 閻羅
  # 或是使用 ID / 英文名稱 / 多個角色
  py generate_tokens.py --roles 閻羅,廚師
  py generate_tokens.py --name Yanluo
  ```
- **視覺效果**：
  - 套用 **標楷體**，並根據字數自動以 SVG viewBox 機制自適應縮放，文字絕不遮擋頭像。
  - 加入 **3D 水晶高光漸層遮罩**，使令片呈現立體玻璃/滴膠質感。
  - 啟用 **`vintage-ink` 復古油墨濾鏡**，使筆畫邊緣磨損、字體內部呈現如活字印刷的油墨斑駁空洞。
  - **自動除黑邊**，使令片外圍 100% 乾淨透明。
- **產出位置**：所有圖片會保存在根目錄的 **`Token_png`** 資料夾中，檔名格式為 `[陣營]_[角色名稱].png`。

---

### 步驟二：A4 實體列印排版拼接 (`layout_tokens.py`)

本步驟會將 `Token_png` 資料夾下的所有圖片進行陣營排序（鎮民 $\rightarrow$ 外來者 $\rightarrow$ 爪牙 $\rightarrow$ 惡魔 $\rightarrow$ 旅客 $\rightarrow$ 傳奇 $\rightarrow$ 特殊），拼貼到 600 DPI 的 A4 高清畫布（$4960 \times 7016$ 像素）上，並自動在周圍繪製極淡灰色裁剪線。

- **基本執行指令**：
  ```bash
  py layout_tokens.py
  ```
- **產出位置**：排版好的多頁 A4 圖片將存儲在根目錄的 **`Token_A4`** 資料夾中（檔名如 `A4_Page_1.png` 等，每張約 10MB）。

---

## 🎛️ 步驟二：命令行自定義參數說明

您可以透過命令列引數，自由調整令片的列印直徑、間距和裁剪線，排版器會**自動計算每頁的行列分佈**，完全不需手動調整程式碼。

### 可用參數表：

| 參數選項 | 說明 | 預設值 |
| :--- | :--- | :--- |
| `--size` | 令片實體目標直徑 (單位：mm) | `25.0` |
| `--gap` | 令片與令片之間的實體間距 (單位：mm) | `3.0` |
| `--offset` | 裁剪線向外推的像素值 (單位：像素 px) | `2` |
| `--team` | 只排版特定陣營角色 (支援多個，以逗號隔開，如 `爪牙,惡魔` / `minion,demon`) | `None` (全部) |
| `--roles` | 只排版特定角色名稱 (支援多個，以英文逗號隔開) | `None` (全部) |
| `--back-offset-x` | 背面圖檔水平物理偏移補償 (單位：mm，正值向右，負值向左) | `0.0` |
| `--back-offset-y` | 背面圖檔垂直物理偏移補償 (單位：mm，正值向下，負值向上) | `0.0` |
| `--flip` | 背面翻轉鏡像模式：`long`（長邊/左右翻轉，預設）或 `short`（短邊/上下翻轉） | `long` |

### 💡 實用場景與執行指令：

#### 1. 標準預設列印 (直徑 28mm，相隔 2mm)
最適合市面上標準 25mm 圓形切卡器裁切的尺寸。一共會生成 **4 頁 A4 紙張**。
```bash
py layout_tokens.py
```

#### 2. 省紙列印版 (直徑 28mm，相隔 2mm)
如果您想節省列印成本，此參數會自動將版面提升為 **8 列 x 12 行 = 96 個/頁**，直接將 268 個角色壓縮排列至 **3 頁 A4 紙張**（直接省下 1 頁白紙，且 24mm 尺寸幾乎不變小）。
```bash
py layout_tokens.py --size 28 --gap 2
```

#### 3. 裁剪線間距拉大
如果您希望裁剪線離令片邊緣再多空出一些白色安全距離（例如 0.5mm 左右），可將 offset 參數調大：
```bash
py layout_tokens.py --offset 6
```

#### 4. 單獨補印特定陣營（支援多個陣營，例如：只想列印「爪牙」與「惡魔」）
只會將指定的陣營角色單獨排版（自動清空原本的舊頁面，只保留生成的頁面）：
```bash
py layout_tokens.py --team 爪牙,惡魔
# 或是輸入英文： py layout_tokens.py --team minion,demon
```

#### 5. 特定角色遺失，進行單獨補印（例如：只想列印「廚師」和「投毒者」）
只會把您指定的這兩個角色令片單獨排在 **1 頁 A4 紙**上進行局部列印。
```bash
py layout_tokens.py --roles 廚師,投毒者
```

#### 6. 日後新增角色：秒級增量生成與自動只印新角色 (推薦 workflow)
當您日後在 JSON 檔案中新增了新角色，不想重複下載舊圖檔也不想重新列印整套舊 A4 頁面時：

1. **僅生成新增的角色圖檔 (自動跳過已存在的舊圖檔)**：
   ```bash
   py generate_tokens.py --skip-existing
   ```
2. **自動僅將未排版的新角色單獨組版輸出 (只印新角色，不覆蓋舊頁面)**：
   ```bash
   py layout_tokens.py --new-only
   ```
   *執行後會在 `Token_A4` 資料夾生成 `A4_Page_New_1.png` 與 `A4_Page_New_1_Back.png`，您只需列印這個新增的 A4 頁面即可完成補印！*

---

## ⚠️ 注意事項,

1. **圖片快取**：下載的角色頭像會快取在 `public/pic/temp_roles` 資料夾中。日後重新執行步驟一時，將免去重複下載時間，實現秒級生成。若想重新下載所有頭像，只需刪除此快取資料夾即可。
2. **自動清空機制**：兩個腳本在啟動時都會自動清空各自對應輸出目錄（`Token_png` 或 `Token_A4`）下的舊 PNG 圖檔，防止新舊規格的檔案混合。請確保您已備份了裡面有需要保留的自建圖片。
